// - On sub - check the code
// - If code is correct then pass
// - If code is not correct then no pass (do what?



//
// function checkCode(userCode, setCode) {
//   if (userCode == setCode) {
//     // Do something
//   } else {
//     // Do something else
//   };
// }

Notification.requestPermission(function(status) {
    console.log('Notification permission status:', status);
});
